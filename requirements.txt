# Core data and ML (Stages 2-7)
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
joblib>=1.4          # save and load trained models

# Exploration (Stage 3)
matplotlib>=3.8
jupyter>=1.0
tornado<6.5           # pin: 6.5+ breaks static file serving in jupyter_server 2.21

# API (Stage 8)
fastapi>=0.115
uvicorn>=0.30        # the server that runs the FastAPI app
pydantic>=2.7        # request/response validation

# Testing
pytest>=8.0
httpx>=0.27          # needed by FastAPI's TestClient

# LLM explanation (Stage 10)
anthropic>=0.40
python-dotenv>=1.0   # load ANTHROPIC_API_KEY from .env

# UI (Stage 12)
streamlit>=1.38
